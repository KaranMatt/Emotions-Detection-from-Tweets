tensorflow>=2.10.0   # LSTM, GRU, BiLSTM models, TextVectorization and Embedding layers
scikit-learn>=1.3.0  # Class weight computation, classification report, train/val split
pandas>=2.0.0        # Data loading and manipulation
numpy>=1.24.0        # Numerical computing and array operations
mlflow>=2.5.0        # Experiment tracking for all model runs and hyperparameters
dvc>=3.0.0           # Version control for dataset (data.dvc) and models (models.dvc)
fastapi>=0.110.0     # REST API framework (/predict endpoint serving BiLSTM model)
uvicorn>=0.29.0      # ASGI server to run FastAPI
pydantic>=2.0.0      # Request/response schema validation
